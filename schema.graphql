type Query {
  artists(filter: String): [Artist!]!
  artist(id: Int!): Artist
}

type Artist {
  id: ID!
  name: String!
  albums: [Album!]!
}

type Album {
  id: ID
  title: String
  year: Int
  tracks: [Track!]!
}

type Track {
  id: ID
  title: String
  duration: Int
}
